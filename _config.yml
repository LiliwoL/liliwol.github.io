# Theme GitHub Pages
remote_theme: pages-themes/leap-day@v0.2.0
plugins:
- jekyll-remote-theme # add this line to the plugins list if you already have one

# Variables de conf
title: Supports de formation
author:
  name: LiliwoL
  email: formation@liliwol.fr
description: >
  Supports de formation BTS SIO

minima:
  date_format: "%b %-d, %Y"

domain: liliwol.github.io       # if you want to force HTTPS, specify the domain without the http at the start, e.g. example.com
url: https://liliwol.github.io  # the base hostname and protocol for your site, e.g. http://example.com
baseurl: /      # place folder name if the site is served in a subfolder
